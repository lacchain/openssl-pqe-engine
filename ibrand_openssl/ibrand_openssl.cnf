# This openssl configuration file enables the IronBridge ibrand RNG.

openssl_conf = openssl_def

[openssl_def]
engines = engine_section

[engine_section]
ibrand = ibrand_section

[ibrand_section]
engine_id = ibrand_openssl
default_algorithms = ALL
init = 0
